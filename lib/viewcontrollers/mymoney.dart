import 'package:flutter/material.dart';

class MyMoneyVC extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('MYMONE'),
      ),
    );
  }
}