import 'package:flutter/material.dart';

class MyLifeVC extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('MyLifeVC'),
      ),
    );
  }
}